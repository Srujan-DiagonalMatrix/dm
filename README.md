# JAI WordPress landing page

A custom, responsive WordPress theme recreating the supplied JAI landing-page reference.

## Run locally

```bash
docker compose up -d
```

Open <http://localhost:8080>, complete the WordPress installer, then activate **JAI Landing Page** under Appearance → Themes.
